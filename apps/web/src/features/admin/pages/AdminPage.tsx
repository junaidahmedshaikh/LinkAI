export default function AdminPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-amber-200">Admin Panel</h2>
      <p className="mt-2 text-sm text-muted-foreground">
        Admin features are prepared for a future phase. Role-based access is active.
      </p>
    </div>
  );
}
